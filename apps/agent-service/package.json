{
  "name": "agent-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "agents:manifest": "node scripts/generate-agent-manifest.mjs",
    "predev": "pnpm agents:manifest",
    "prebuild": "pnpm agents:manifest",
    "preprepare": "pnpm agents:manifest",
    "pretypecheck": "pnpm agents:manifest",
    "dev": "nitropack dev",
    "build": "nitropack build",
    "prepare": "nitropack prepare",
    "preview": "node .output/server/index.mjs",
    "typecheck": "nitropack prepare && tsc --noEmit",
    "test": "tsx --test src/lib/loop-event-to-ui-chunk.test.ts src/workflows/agent-loop.helpers.test.ts src/lib/tool-strictness.test.ts"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^3.0.83",
    "@eloquio/agent-harness": "workspace:*",
    "@eloquio/db-schema": "workspace:*",
    "@eloquio/lattik-expression": "workspace:*",
    "@eloquio/render-intents": "workspace:*",
    "ai": "^6.0.141",
    "drizzle-orm": "^0.45.2",
    "h3": "^1.15.0",
    "jose": "^6.0.0",
    "postgres": "^3.4.8",
    "workflow": "^4.2.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "nitropack": "^2.13.4",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
